[package]
name = "node"
version = "0.1.9"
edition = "2024"

[[bin]]
name = "node"
path = "src/main.rs"

[dependencies]
cfx_core = { path = "../core" }
clap = { version = "4.5.60", features = ["derive"] }
tokio = { version = "1.49.0", features = ["rt-multi-thread", "macros", "time", "signal"] }

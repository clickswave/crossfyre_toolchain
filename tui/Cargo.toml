[package]
name = "cfx_tui"
version = "0.1.0"
edition = "2024"

[dependencies]
ratatui = { version = "0.30.0-alpha.5", features = ["crossterm"] }
crossterm = "0.29.0"

[features]
default = []
# Adds the live tuning panel: concurrency, pacing and posture as the run
# adapts. Off in published builds. See the note in src/tuning.rs.
tuning = []
